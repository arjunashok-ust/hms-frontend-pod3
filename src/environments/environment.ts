/* Default (development) environment.
   Replaced by environment.prod.ts in production builds via angular.json. */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:5000',
};

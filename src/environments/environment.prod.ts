/* Production environment.
   Set apiBaseUrl to your deployed API origin (e.g. https://api.yourdomain.com)
   before building for production. */
export const environment = {
  production: true,
  apiBaseUrl: '3.106.77.205',
};

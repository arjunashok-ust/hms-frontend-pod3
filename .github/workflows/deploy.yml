name: Deploy HMS FRONTEND

on:
  push:
    branches:
      - feature/sprint_5/vinayasri

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy EC2 FRONTEND
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_VINAYA_HOST }}
          username: ${{ secrets.EC2_VINAYA_USER }}
          key: ${{ secrets.EC2_VINAYA_SSH_KEY }}
          script: |
            cd hms-frontend-pod3
            git pull origin feature/sprint_5/vinayasri
            sudo npm ci

            ng build --configuration production
            rm -rf /var/www/hms-frontend/*
            cp -r dist/hms-frontend/browser/* /var/www/html/
            sudo systemctl restart nginx